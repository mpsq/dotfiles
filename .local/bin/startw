#!/usr/bin/env bash
set -euo pipefail

dbus-update-activation-environment --systemd DISPLAY SWAYSOCK WAYLAND_DISPLAY XDG_CURRENT_DESKTOP SSH_AUTH_SOCK
systemctl --user --wait start sway-session.target
