[Unit]
Description=Reload gpg-agent with Wayland environment
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=/usr/bin/gpgconf --kill gpg-agent

[Install]
WantedBy=graphical-session.target
